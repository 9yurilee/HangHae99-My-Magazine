import React from "react";
import Post from '../components/Post';
import styled from 'styled-components';

const Main = (props) => {
  return (
    <div>
      <Post />
    </div>
  )
}
export default Main;